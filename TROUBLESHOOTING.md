# 🔍 ЧЕКЛИСТ ПРОВЕРКИ - ПОШАГОВО

## Шаг 1: Проверка файлов

Убедитесь что эти файлы существуют:

```bash
# Проверить наличие модалки запросов
ls components/modals/SwapRequestsModal.tsx

# Проверить наличие модалки создания обмена
ls components/modals/ScheduleSwapModal.tsx

# Проверить наличие API
ls app/api/schedule-swap/route.ts
ls app/api/schedule-swap/[id]/route.ts
```

**Ожидаемый результат:** Все файлы должны существовать

---

## Шаг 2: Проверка миграции БД

```bash
# Проверить что таблица создана
npx prisma studio
```

Откроется браузер. Проверьте:
- ✅ Должна быть таблица `ScheduleSwapRequest`
- ✅ Enum `AttendanceStatus` должен содержать `WEEKEND`

**Если таблицы НЕТ:**
```bash
npx prisma migrate dev --name add_schedule_swap
npx prisma generate
```

---

## Шаг 3: Очистить кэш и перезапустить

```bash
# Остановить сервер (Ctrl+C)

# Удалить кэш
rm -rf .next

# Переустановить зависимости (если нужно)
npm install

# Запустить заново
npm run dev
```

---

## Шаг 4: Проверка в браузере

### Для сотрудника (НЕ админа):

1. Войти как обычный пользователь:
   - Email: `user@company.com`
   - Пароль: `user123`

2. Открыть главную страницу (Календарь)

3. **ПРОВЕРИТЬ:** Справа от поиска должна быть фиолетовая кнопка "🔄 Запросы"

4. Нажать на кнопку "Запросы"

5. **ПРОВЕРИТЬ:** Должно открыться модальное окно с вкладками:
   - "⬅ Входящие"
   - "➡ Исходящие"

### Для админа:

1. Войти как админ:
   - Email: `admin@company.com`
   - Пароль: `admin123`

2. Открыть страницу "Запросы" в меню

3. **ПРОВЕРИТЬ:** Должны быть вкладки:
   - Все
   - Отпуска
   - Сверхурочные
   - Изменения
   - **Обмены** ← ЕСЛИ НЕТ, см. ADMIN_SWAP_TAB.md

---

## 🐛 Если кнопка "Запросы" не появилась:

### Проверка 1: Файл page.tsx обновлен?

```bash
grep "SwapRequestsModal" app/\(dashboard\)/page.tsx
```

Должно вывести несколько строк с упоминанием SwapRequestsModal.

**Если ничего не вывело:**
```bash
# Файл не обновлен, нужно распаковать архив заново
unzip -o employee-attendance-system.zip
```

### Проверка 2: Роль пользователя

```bash
# Проверить роль через Prisma Studio
npx prisma studio
```

Откройте таблицу `User`, найдите вашего пользователя.
- Если `role = ADMIN` - кнопка "Запросы" НЕ появится (для админа другой интерфейс)
- Если `role = USER` - кнопка должна быть

### Проверка 3: Консоль браузера

Откройте DevTools (F12) → вкладка Console

**Проверить:**
- Нет ли ошибок импорта SwapRequestsModal
- Нет ли ошибок TypeScript

**Если есть ошибка `Cannot find module SwapRequestsModal`:**
```bash
# Перезапустить сервер
rm -rf .next
npm run dev
```

---

## 🐛 Если модальное окно не открывается:

### Проверка 1: Ошибки в консоли

Откройте F12 → Console, нажмите кнопку "Запросы"

**Возможные ошибки:**

1. `Cannot read properties of undefined (reading 'create')`
   - **Решение:** Не применена миграция БД
   ```bash
   npx prisma migrate dev --name add_schedule_swap
   npx prisma generate
   ```

2. `Module not found: SwapRequestsModal`
   - **Решение:** Файл не на месте или не перезапущен сервер
   ```bash
   ls components/modals/SwapRequestsModal.tsx
   rm -rf .next
   npm run dev
   ```

3. `session.user.id is undefined`
   - **Решение:** Перелогиниться
   ```bash
   # Выйти и войти заново
   ```

---

## 📋 Быстрая диагностика

Выполните этот скрипт для полной проверки:

```bash
#!/bin/bash
echo "🔍 Проверка системы..."
echo ""

echo "1. Проверка файлов:"
[ -f "components/modals/SwapRequestsModal.tsx" ] && echo "✅ SwapRequestsModal.tsx" || echo "❌ SwapRequestsModal.tsx ОТСУТСТВУЕТ"
[ -f "components/modals/ScheduleSwapModal.tsx" ] && echo "✅ ScheduleSwapModal.tsx" || echo "❌ ScheduleSwapModal.tsx ОТСУТСТВУЕТ"
[ -f "app/api/schedule-swap/route.ts" ] && echo "✅ API route" || echo "❌ API route ОТСУТСТВУЕТ"
echo ""

echo "2. Проверка импортов:"
grep -q "SwapRequestsModal" "app/(dashboard)/page.tsx" && echo "✅ Импорт в page.tsx" || echo "❌ Импорт ОТСУТСТВУЕТ"
echo ""

echo "3. Проверка БД:"
npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM information_schema.tables WHERE table_name='schedule_swap_requests';" 2>/dev/null && echo "✅ Таблица БД" || echo "❌ Таблица НЕ СОЗДАНА"
echo ""

echo "Если есть ❌ - следуйте инструкциям выше"
```

Сохраните как `check.sh`, дайте права:
```bash
chmod +x check.sh
./check.sh
```

---

## ✅ Контрольные точки:

- [ ] Файлы модалок существуют
- [ ] Таблица `schedule_swap_requests` создана в БД
- [ ] Импорт `SwapRequestsModal` есть в `page.tsx`
- [ ] Кэш `.next` удален
- [ ] Сервер перезапущен
- [ ] Вошли как USER (не ADMIN)
- [ ] Кнопка "Запросы" видна на странице календаря
- [ ] Модальное окно открывается
- [ ] Вкладки "Входящие" и "Исходящие" переключаются

---

## 🆘 Если ничего не помогло:

1. Полный сброс:
```bash
# Остановить сервер
# Удалить всё
rm -rf .next node_modules

# Переустановить
npm install

# Применить миграцию
npx prisma migrate dev --name add_schedule_swap
npx prisma generate

# Запустить
npm run dev
```

2. Проверить версию Node.js:
```bash
node --version  # Должно быть >= 18
```

3. Проверить логи Next.js при запуске на наличие ошибок компиляции

---

После прохождения всех шагов кнопка "Запросы" должна появиться! 🎉
