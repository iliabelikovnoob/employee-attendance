# 🔧 Инструкция по запуску Базы Знаний

## Проблема

После добавления новых моделей в Prisma schema необходимо регенерировать Prisma Client, чтобы TypeScript и API endpoints могли работать с новыми таблицами KB.

## ✅ Решение (выполните на вашем Mac)

### 1. Остановите dev сервер (если запущен)
Нажмите `Ctrl+C` в терминале где запущен `npm run dev`

### 2. Регенерируйте Prisma Client

```bash
cd ~/Downloads/employee-attendance-system
npx prisma generate
```

Вы должны увидеть:
```
✔ Generated Prisma Client (v5.x.x) to ./node_modules/@prisma/client
```

### 3. Перезапустите dev сервер

```bash
npm run dev
```

### 4. Откройте приложение в браузере

```
http://localhost:3000
```

## 🎉 Что теперь доступно

### Для всех пользователей:
- **База Знаний** - новый пункт в навигации
- Просмотр опубликованных статей
- Создание статей (черновики, отправка на модерацию)
- Поиск статей
- Избранное

### Для админов:
- **Категории БЗ** - управление категориями (в меню Аналитика)
- Модерация статей (публикация/отклонение)
- Создание категорий с иконками
- Иерархия категорий (родитель/потомок)

## 📝 Как создать первую категорию

1. Войдите как ADMIN
2. Перейдите: **Аналитика → Категории БЗ**
3. Нажмите **"Создать категорию"**
4. Заполните форму:
   - Название: например "Инструкции"
   - Иконка: 📘 (любой emoji)
   - Описание: краткое описание
5. Нажмите **"Создать"**

## 📚 Как создать первую статью

1. Перейдите: **База Знаний**
2. Нажмите **"Создать статью"**
3. Заполните форму:
   - Заголовок: название статьи
   - Категория: выберите созданную категорию
   - Теги: через запятую (опционально)
   - Содержимое: текст в формате Markdown
4. Выберите действие:
   - **Сохранить черновик** - статья будет доступна только вам
   - **Отправить на модерацию** - админ сможет её опубликовать
   - **Опубликовать сразу** (только для админа) - статья сразу станет доступна всем

## 🔍 Возможности поиска

- Поиск с автодополнением (debounce 300ms)
- Поиск по заголовку и содержимому
- Preview найденного текста

## ⭐ Избранное

- Кнопка-звёздочка на странице статьи
- Блок "Мои избранные" на главной странице БЗ

## 🎯 Workflow статей

```
DRAFT (черновик)
  ↓
PENDING (на модерации)
  ↓ ↘
PUBLISHED   REJECTED
(опубликовано) (отклонено)
```

## ❓ Проблемы?

Если после `npx prisma generate` все еще ошибки:

1. Удалите кэш Next.js:
```bash
rm -rf .next
```

2. Перезапустите dev сервер:
```bash
npm run dev
```

---

**Готово!** База Знаний полностью функциональна 🚀
