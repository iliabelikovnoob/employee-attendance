# 🌙 Обновление: Темная тема

## ⚠️ ВАЖНО! Перед запуском:

После распаковки архива **ОБЯЗАТЕЛЬНО** выполните:

```bash
# 1. Удалите папку .next (кэш Next.js)
rm -rf .next

# 2. Очистите кэш npm (опционально, но рекомендуется)
npm cache clean --force

# 3. Переустановите зависимости (если добавлялись новые)
npm install

# 4. Запустите сервер разработки
npm run dev
```

## 🎨 Что добавлено:

### 1. Темная тема
- Полная поддержка dark mode
- Кнопка переключения в header (иконка солнца/луны)
- Сохранение темы в localStorage
- Автоопределение системной темы

### 2. Новые файлы:
- `contexts/ThemeContext.tsx` - контекст управления темой
- `components/ClientHeaderWrapper.tsx` - обертка для Header

### 3. Обновленные файлы:
- `components/Providers.tsx` - добавлен ThemeProvider
- `components/Header.tsx` - поддержка темной темы, кнопка переключения
- `app/layout.tsx` - удален дублирующий Toaster
- `app/(dashboard)/layout.tsx` - использует ClientHeaderWrapper, темный фон
- `app/globals.css` - стили для темной темы
- `tailwind.config.js` - включен darkMode: 'class'

## 🚀 Использование:

После запуска сервера в правом верхнем углу header появится кнопка переключения темы:
- 🌙 Иконка луны - переключить на темную тему
- ☀️ Иконка солнца - переключить на светлую тему

Тема автоматически сохраняется и применяется при следующем входе.

## 🔧 Если возникают ошибки:

### Ошибка: "useTheme must be used within ThemeProvider"

**Решение:**
```bash
# Остановите сервер (Ctrl+C)
rm -rf .next
npm run dev
```

### Ошибка: "Module not found: Can't resolve '@/contexts/ThemeContext'"

**Решение:**
```bash
# Убедитесь, что папка contexts создана
ls -la contexts/

# Если её нет, проверьте что вы распаковали все файлы из архива
```

## 📋 Структура провайдеров:

```
app/layout.tsx (Root)
  └─ Providers
      └─ SessionProvider
          └─ ThemeProvider ← контекст темы доступен здесь
              └─ app/(dashboard)/layout.tsx
                  └─ ClientHeaderWrapper
                      └─ Header ← использует useTheme()
```

## ✅ Проверка что всё работает:

1. Запустите `npm run dev`
2. Откройте http://localhost:3000
3. Войдите в систему
4. Увидите кнопку с иконкой луны/солнца в header
5. Нажмите на неё - тема должна переключиться
6. Обновите страницу - тема должна сохраниться

Готово! 🎉
