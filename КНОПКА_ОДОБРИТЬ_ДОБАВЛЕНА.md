# ✅ Добавлена кнопка "Одобрить и опубликовать"

**Дата:** 12 февраля 2026
**Изменение:** Добавлена кнопка для быстрого одобрения статей на модерации

---

## 🎯 Что добавлено:

### Кнопка "Одобрить и опубликовать"
- ✅ Появляется в меню действий администратора (⋮)
- ✅ Показывается только для статей со статусом `PENDING`
- ✅ Доступна только администраторам
- ✅ Одним кликом публикует статью

---

## 🔧 Как работает:

### Для администратора:
1. Открывает статью со статусом "На модерации"
2. Видит желтый бейдж "⏳ На модерации"
3. Нажимает на меню действий (⋮)
4. Видит кнопку "✓ Одобрить и опубликовать" (зелёная, вверху меню)
5. Нажимает на кнопку
6. Подтверждает действие в диалоге
7. ✅ Статья публикуется, статус меняется на `PUBLISHED`

### Что происходит при одобрении:
1. Отправляется запрос `POST /api/kb/articles/:id/publish`
2. API изменяет статус статьи на `PUBLISHED`
3. Показывается уведомление "Статья одобрена и опубликована!"
4. Статус обновляется локально (бейдж исчезает)
5. Обновляется сайдбар (статья появляется в общем списке)

---

## 📝 Технические детали:

### Добавленная функция:
```typescript
// /app/(dashboard)/knowledge-base/article/[slug]/page.tsx

const handleApprove = async () => {
  if (!article) return;

  if (!confirm('Одобрить и опубликовать эту статью?')) return;

  try {
    const response = await fetch(`/api/kb/articles/${article.id}/publish`, {
      method: 'POST',
    });

    if (response.ok) {
      alert('Статья одобрена и опубликована!');
      // Обновляем статус локально
      setArticle({ ...article, status: 'PUBLISHED' });
      // Обновляем сайдбар
      window.dispatchEvent(new Event('kb-refresh'));
    } else {
      const error = await response.json();
      alert(`Ошибка: ${error.error || 'Не удалось опубликовать'}`);
    }
  } catch (error) {
    console.error('Error approving article:', error);
    alert('Ошибка при публикации');
  }
};
```

### Добавленная кнопка в меню:
```typescript
{/* Кнопка одобрения (только для админов и статей PENDING) */}
{isAdmin && article.status === 'PENDING' && (
  <>
    <button
      onClick={() => {
        handleApprove();
        setShowActionsMenu(false);
      }}
      className="w-full flex items-center gap-3 px-4 py-2 text-sm text-green-600 dark:text-green-400 hover:bg-green-50 dark:hover:bg-green-900/20 transition-colors text-left"
    >
      <IoCheckmarkCircleOutline className="w-5 h-5" />
      <div>
        <div className="font-medium">Одобрить и опубликовать</div>
        <div className="text-xs text-green-500 dark:text-green-400 opacity-75">
          Сделать статью доступной всем
        </div>
      </div>
    </button>
    <div className="my-1 border-t border-gray-200 dark:border-gray-700"></div>
  </>
)}
```

### Условия отображения:
- `isAdmin` - пользователь должен быть администратором
- `article.status === 'PENDING'` - статья должна быть на модерации

### Используемый API:
- `POST /api/kb/articles/:id/publish`
- Требует роль `ADMIN`
- Изменяет статус на `PUBLISHED`

---

## 🎨 Визуальный дизайн:

### Цветовая схема:
- **Зелёный цвет** - для кнопки одобрения (позитивное действие)
- **Иконка ✓** - IoCheckmarkCircleOutline
- **Hover эффект** - светло-зелёный фон

### Положение в меню:
1. ✓ **Одобрить и опубликовать** (если PENDING, только админ)
2. --- (разделитель)
3. 🌿 История версий
4. ✏️ Редактировать
5. --- (разделитель)
6. 🗑️ Удалить

---

## 🚀 Рабочий процесс:

### Сценарий 1: Быстрое одобрение со страницы "Ждут публикации"
1. Админ открывает `/knowledge-base/pending`
2. Видит список статей на модерации
3. Нажимает "Просмотреть и одобрить"
4. Читает статью
5. Открывает меню действий (⋮)
6. Нажимает "Одобрить и опубликовать"
7. ✅ Готово!

### Сценарий 2: Одобрение с правками
1. Админ открывает статью на модерации
2. Видит ошибки/опечатки
3. Нажимает "Редактировать"
4. Исправляет ошибки
5. Сохраняет изменения
6. Возвращается к статье
7. Открывает меню (⋮)
8. Нажимает "Одобрить и опубликовать"
9. ✅ Готово!

### Сценарий 3: Отклонение вместо одобрения
1. Админ открывает статью на модерации
2. Находит серьёзные проблемы
3. Открывает меню (⋮)
4. Нажимает "Удалить" или использует функцию отклонения
5. Указывает причину
6. ✅ Статья отклонена

---

## ⚡ Преимущества:

1. **Быстрое одобрение:**
   - ✅ Одна кнопка вместо перехода на другую страницу
   - ✅ Мгновенная публикация
   - ✅ Обновление в реальном времени

2. **Удобство для админов:**
   - ✅ Не нужно искать отдельную кнопку
   - ✅ Всё в одном месте (меню действий)
   - ✅ Понятная иконка и текст

3. **Безопасность:**
   - ✅ Подтверждение перед публикацией
   - ✅ Только админы могут одобрять
   - ✅ API проверяет права доступа

4. **UX:**
   - ✅ Зелёный цвет = позитивное действие
   - ✅ Разделитель отделяет от других действий
   - ✅ Кнопка вверху меню (приоритетное действие)

---

## 📊 Итоги:

### До изменений:
- ❌ Не было быстрого способа одобрить статью
- ❌ Приходилось переходить в редактор или использовать другие страницы

### После изменений:
- ✅ Кнопка "Одобрить и опубликовать" в меню действий
- ✅ Быстрое одобрение прямо со страницы статьи
- ✅ Автоматическое обновление статуса

---

*Функционал добавлен!*
*Версия: 2.3*
*Дата: 12.02.2026*
