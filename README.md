# 📅 Система учета присутствия сотрудников

Современное веб-приложение для отслеживания местоположения сотрудников с интерактивным календарем.

## 🚀 Технологии

- **Frontend:** Next.js 14, React 18, TypeScript
- **Styling:** Tailwind CSS, Framer Motion
- **Backend:** Next.js API Routes
- **Database:** PostgreSQL + Prisma ORM
- **Authentication:** NextAuth.js
- **UI Components:** Custom components с Apple-стилем дизайна

## 📋 Функционал

### Для всех пользователей:
- 📆 Интерактивный календарь с месячным и годовым представлением
- 👥 Просмотр статусов всех сотрудников
- 🎨 Цветовая индикация статусов (В офисе, Из дома, Больничный, Отпуск, Отгул)
- 📱 Адаптивный дизайн для мобильных устройств

### Для сотрудников (USER):
- 📝 Запросы на изменение своего статуса
- 💬 Указание причины для запроса

### Для администраторов (ADMIN):
- ✅ Управление статусами всех сотрудников
- 👤 CRUD операции с сотрудниками (создание, редактирование, удаление)
- 📸 Загрузка аватарок
- 🔔 Просмотр и обработка запросов на изменение статусов
- 📊 Полный контроль над системой

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 18+ 
- PostgreSQL 14+
- npm или yarn

### Шаг 1: Клонирование и установка зависимостей

\`\`\`bash
cd employee-attendance-system
npm install
\`\`\`

### Шаг 2: Настройка базы данных PostgreSQL

1. Установите PostgreSQL если еще не установлен:

**macOS (с Homebrew):**
\`\`\`bash
brew install postgresql@14
brew services start postgresql@14
\`\`\`

**Ubuntu/Debian:**
\`\`\`bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
\`\`\`

**Windows:**
Скачайте установщик с https://www.postgresql.org/download/windows/

2. Создайте базу данных:

\`\`\`bash
# Войдите в PostgreSQL
psql postgres

# Создайте пользователя и базу данных
CREATE USER postgres WITH PASSWORD 'postgres';
CREATE DATABASE attendance_db;
GRANT ALL PRIVILEGES ON DATABASE attendance_db TO postgres;
\\q
\`\`\`

### Шаг 3: Настройка переменных окружения

Файл `.env` уже создан в корне проекта со следующим содержимым:

\`\`\`env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/attendance_db?schema=public"
NEXTAUTH_SECRET="your-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
\`\`\`

**Важно:** Если вы использовали другие учетные данные для PostgreSQL, обновите `DATABASE_URL`.

Для production генерируйте безопасный NEXTAUTH_SECRET:
\`\`\`bash
openssl rand -base64 32
\`\`\`

### Шаг 4: Инициализация базы данных

\`\`\`bash
# Генерация Prisma Client
npx prisma generate

# Применение схемы к базе данных
npx prisma db push

# Заполнение тестовыми данными
npm run prisma:seed
\`\`\`

После выполнения seed будут созданы:
- Администратор: admin@company.com / admin123
- Сотрудник 1: user@company.com / user123
- Сотрудник 2: maria@company.com / user123
- Сотрудник 3: alex@company.com / user123
- Тестовые записи посещаемости на текущий месяц
- Один тестовый запрос на изменение статуса

### Шаг 5: Запуск приложения

\`\`\`bash
# Development mode с hot reload
npm run dev

# Или для production
npm run build
npm start
\`\`\`

Приложение будет доступно по адресу: **http://localhost:3000**

### Шаг 6: Вход в систему

Откройте http://localhost:3000 в браузере. Вы увидите страницу входа.

**Тестовые аккаунты:**
- **Администратор:** admin@company.com / admin123
- **Сотрудник:** user@company.com / user123

## 📁 Структура проекта

\`\`\`
employee-attendance-system/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # Auth pages
│   │   └── login/               # Страница входа
│   ├── (dashboard)/             # Protected pages
│   │   ├── employees/           # Управление сотрудниками
│   │   ├── requests/            # Запросы на изменения
│   │   ├── layout.tsx           # Dashboard layout
│   │   └── page.tsx             # Главная страница (календарь)
│   ├── api/                     # API routes
│   │   ├── auth/               # NextAuth endpoints
│   │   ├── users/              # User CRUD
│   │   ├── attendance/         # Attendance management
│   │   └── requests/           # Change requests
│   ├── globals.css             # Глобальные стили
│   └── layout.tsx              # Root layout
├── components/                  # React components
│   ├── calendar/               # Календарные компоненты
│   │   ├── Calendar.tsx        # Главный календарь
│   │   ├── DayCell.tsx         # Ячейка дня
│   │   └── YearView.tsx        # Годовое представление
│   ├── modals/                 # Модальные окна
│   │   ├── DayDetailsModal.tsx       # Детали дня
│   │   ├── UpdateStatusModal.tsx     # Обновление статусов (админ)
│   │   ├── RequestChangeModal.tsx    # Запрос изменения (user)
│   │   └── UserFormModal.tsx         # Форма сотрудника
│   ├── ui/                     # UI компоненты
│   │   ├── Modal.tsx           # Базовый модальный компонент
│   │   └── Button.tsx          # Кнопка
│   ├── Header.tsx              # Шапка приложения
│   └── Providers.tsx           # Session Provider
├── lib/                        # Утилиты и конфигурация
│   ├── prisma.ts              # Prisma client
│   ├── auth.ts                # NextAuth config
│   └── calendar.ts            # Календарные утилиты
├── prisma/                     # Prisma
│   ├── schema.prisma          # Database schema
│   └── seed.ts                # Seed данные
├── types/                      # TypeScript types
│   ├── index.ts               # Основные типы
│   └── next-auth.d.ts         # NextAuth типы
├── public/                     # Статические файлы
│   └── uploads/               # Загруженные аватарки
├── .env                        # Environment variables
├── package.json               # Dependencies
└── tsconfig.json              # TypeScript config
\`\`\`

## 🗄️ Схема базы данных

### Таблицы

**users** - Пользователи системы
- id, email, password, role (ADMIN/USER)
- name, phone, position, skills, avatar
- createdAt, updatedAt

**attendances** - Записи о посещаемости
- id, userId (→ users), date, status
- status: OFFICE, REMOTE, SICK, VACATION, DAYOFF
- Unique constraint на (userId, date)

**attendance_requests** - Запросы на изменение
- id, userId (→ users), date, newStatus, reason
- status: PENDING, APPROVED, REJECTED
- createdAt, updatedAt

### Связи
- User → Attendance (One-to-Many)
- User → AttendanceRequest (One-to-Many)
- Каскадное удаление при удалении пользователя

## 🎨 Дизайн и UI

### Цветовая схема статусов:
- 🟢 **В офисе** - #10b981 (зеленый)
- 🟡 **Из дома** - #f59e0b (желтый)
- 🔴 **Больничный** - #ef4444 (красный)
- 🏖️ **Отпуск** - #3b82f6 (голубой)
- ⚪ **Отгул** - #6b7280 (серый)

### Особенности дизайна:
- Минималистичный Apple-стиль
- Плавные анимации (Framer Motion)
- Адаптивная сетка (Tailwind CSS)
- Модальные окна с backdrop blur
- Кастомная прокрутка

## 🔄 Алгоритм работы

### Отрисовка календаря:

1. **Расчет дней месяца:**
   - Получаем первый день месяца (`startOfMonth`)
   - Получаем последний день месяца (`endOfMonth`)
   - Вычисляем начало календарной сетки (понедельник предыдущей недели)
   - Вычисляем конец календарной сетки (воскресенье следующей недели)
   - Генерируем массив всех дней в этом диапазоне

2. **Получение данных:**
   - Запрос к `/api/attendance?view=month&date=YYYY-MM-DD`
   - Получаем все записи посещаемости за месяц
   - Группируем по дате в объект `{ "2024-02-05": [attendance1, attendance2, ...] }`

3. **Отображение:**
   - Для каждого дня рендерим `DayCell`
   - В ячейке показываем аватарки пользователей (макс 6)
   - Обводка аватарки соответствует статусу пользователя
   - Текущий день подсвечивается синей рамкой

### Flow запроса на изменение:

1. **Сотрудник создает запрос:**
   - Выбирает дату в календаре → "Запросить изменение"
   - Выбирает новый статус и указывает причину
   - POST `/api/requests` → создается запись в `attendance_requests`
   - Статус: `PENDING`

2. **Администратор получает уведомление:**
   - В Header отображается красный бейдж с количеством новых запросов
   - GET `/api/requests?status=PENDING` для получения списка

3. **Обработка запроса:**
   - Админ нажимает "Подтвердить" или "Отклонить"
   - PUT `/api/requests/:id` с action: 'approve' | 'reject'

4. **При подтверждении (транзакция):**
   - Обновляется статус запроса → `APPROVED`
   - Создается/обновляется запись в `attendances`
   - Календарь автоматически обновляется

5. **При отклонении:**
   - Обновляется статус запроса → `REJECTED`
   - Запись в `attendances` не изменяется

## 🔐 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для сессий (NextAuth)
- Protected API routes с проверкой роли
- Валидация данных на сервере
- CSRF защита через NextAuth

## 📱 Адаптивность

- Desktop: полная функциональность
- Tablet: адаптированная сетка
- Mobile: одноколоночный layout
- Touch-friendly интерфейс

## 🐛 Troubleshooting

### База данных не подключается
\`\`\`bash
# Проверьте статус PostgreSQL
# macOS:
brew services list
# Linux:
sudo systemctl status postgresql
# Windows: проверьте Services

# Проверьте правильность DATABASE_URL в .env
# Попробуйте подключиться вручную:
psql -U postgres -d attendance_db
\`\`\`

### Ошибка при prisma generate
\`\`\`bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
npx prisma generate
\`\`\`

### Не загружаются аватарки
\`\`\`bash
# Создайте директорию для загрузок
mkdir -p public/uploads/avatars
\`\`\`

### Ошибка "Module not found"
\`\`\`bash
# Очистите кэш Next.js
rm -rf .next
npm run dev
\`\`\`

## 📚 Дополнительная информация

### Prisma Studio (GUI для БД):
\`\`\`bash
npx prisma studio
# Откроется http://localhost:5555
\`\`\`

### Миграции базы данных:
\`\`\`bash
# Создание миграции
npx prisma migrate dev --name your_migration_name

# Применение в production
npx prisma migrate deploy
\`\`\`

### Очистка базы данных:
\`\`\`bash
npx prisma migrate reset  # Удалит все данные!
npm run prisma:seed       # Восстановит тестовые данные
\`\`\`

## 👨‍💻 Разработка

### Добавление нового статуса:

1. Обновите enum в `prisma/schema.prisma`:
\`\`\`prisma
enum AttendanceStatus {
  // ... существующие
  NEW_STATUS
}
\`\`\`

2. Добавьте перевод в `types/index.ts`:
\`\`\`typescript
export const StatusLabels = {
  // ...
  NEW_STATUS: 'Новый статус',
}

export const StatusColors = {
  // ...
  NEW_STATUS: '#hexcolor',
}
\`\`\`

3. Примените миграцию:
\`\`\`bash
npx prisma db push
\`\`\`

## 📄 Лицензия

Проект создан в учебных целях.

## 🙋‍♂️ Поддержка

Если возникли проблемы с запуском, проверьте:
1. Node.js версии 18 или выше
2. PostgreSQL запущен и доступен
3. Все зависимости установлены
4. .env файл настроен правильно
5. База данных создана и заполнена

---

**Приятного использования! 🎉**
