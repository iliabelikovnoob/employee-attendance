# 📄 Загрузка документов Word и PDF - Инструкция

**Дата:** 12 февраля 2026
**Статус:** В разработке (60% готово)

---

## ✅ Что уже сделано:

### 1. **База данных** ✅
- Добавлен enum `ArticleContentType` (TEXT, DOCX, PDF)
- Обновлена модель `KbArticle`:
  - `contentType` - тип контента
  - `filePath` - путь к файлу
  - `fileSize` - размер файла
  - `originalFileName` - оригинальное имя
- Обновлена модель `KbArticleVersion` для хранения версий файлов
- Схема синхронизирована: `npx prisma db push` ✅

### 2. **API загрузки** ✅
- Создан роут `/api/kb/upload`
- Поддержка `.docx` и `.pdf` файлов
- Конвертация DOCX в HTML через `mammoth`
- Валидация типа и размера файлов (макс 50MB)
- Сохранение в `/public/uploads/kb/documents/`

### 3. **Компонент загрузки** ✅
- `components/kb/FileUpload.tsx`
- Drag-and-drop интерфейс
- Превью загруженного файла
- Индикатор прогресса
- Обработка ошибок

### 4. **Библиотеки** ✅
```bash
npm install mammoth pdf-parse  # Установлено ✅
```

---

## 🚧 Что нужно доделать:

### 1. **Обновить страницу создания статьи**

Файл: `/app/(dashboard)/knowledge-base/new/page.tsx`

Нужно добавить:
- Переключатель "Написать текст" / "Загрузить документ"
- Интеграцию компонента `FileUpload`
- Логику сохранения статьи с файлом

**Пример кода:**

```tsx
const [contentMode, setContentMode] = useState<'text' | 'file'>('text');
const [fileData, setFileData] = useState<any>(null);

// В форме:
<div className="mb-6">
  <label className="block text-sm font-medium mb-2">Тип контента</label>
  <div className="flex gap-4">
    <button
      type="button"
      onClick={() => setContentMode('text')}
      className={`px-4 py-2 rounded-lg ${
        contentMode === 'text'
          ? 'bg-blue-600 text-white'
          : 'bg-gray-200 text-gray-700'
      }`}
    >
      📝 Написать текст
    </button>
    <button
      type="button"
      onClick={() => setContentMode('file')}
      className={`px-4 py-2 rounded-lg ${
        contentMode === 'file'
          ? 'bg-blue-600 text-white'
          : 'bg-gray-200 text-gray-700'
      }`}
    >
      📁 Загрузить документ
    </button>
  </div>
</div>

{contentMode === 'text' ? (
  // Существующий markdown редактор
  <MarkdownEditor />
) : (
  // Компонент загрузки файла
  <FileUpload
    onFileUploaded={(data) => setFileData(data)}
    currentFile={fileData}
    onFileRemove={() => setFileData(null)}
  />
)}
```

**При сохранении статьи:**

```tsx
const handleSubmit = async () => {
  const articleData = {
    title,
    slug,
    categoryId,
    tagIds,
    status,
    // Если загружен файл:
    contentType: fileData ? fileData.contentType : 'TEXT',
    content: fileData?.htmlContent || markdownContent,
    filePath: fileData?.filePath,
    fileSize: fileData?.fileSize,
    originalFileName: fileData?.originalFileName,
  };

  const response = await fetch('/api/kb/articles', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(articleData),
  });
};
```

---

### 2. **Обновить страницу редактирования**

Файл: `/app/(dashboard)/knowledge-base/edit/[id]/page.tsx`

Аналогично странице создания:
- Показывать текущий тип контента
- Позволить заменить файл (с сохранением в версиях)
- При замене файла:
  ```tsx
  // Сохранить старую версию
  const oldVersion = {
    title: article.title,
    content: article.content,
    contentType: article.contentType,
    filePath: article.filePath,
    fileSize: article.fileSize,
    originalFileName: article.originalFileName,
  };

  // Создать запись в KbArticleVersion
  ```

---

### 3. **Обновить страницу просмотра статьи**

Файл: `/app/(dashboard)/knowledge-base/article/[slug]/page.tsx`

Добавить отображение в зависимости от типа контента:

```tsx
// В компоненте просмотра статьи:
{article.contentType === 'TEXT' && (
  <MarkdownRenderer content={article.content} />
)}

{article.contentType === 'DOCX' && (
  <div
    className="prose prose-lg dark:prose-invert max-w-none"
    dangerouslySetInnerHTML={{ __html: article.content }}
  />
)}

{article.contentType === 'PDF' && (
  <div className="w-full h-screen">
    <iframe
      src={article.filePath}
      className="w-full h-full border-0 rounded-lg"
      title={article.title}
    />
  </div>
)}
```

**Опционально:** Добавить кнопку скачивания оригинального файла:

```tsx
{(article.contentType === 'DOCX' || article.contentType === 'PDF') && (
  <a
    href={article.filePath}
    download={article.originalFileName}
    className="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
  >
    <IoDownloadOutline className="w-5 h-5" />
    Скачать оригинал ({article.originalFileName})
  </a>
)}
```

---

### 4. **Обновить API создания/редактирования статей**

Файлы:
- `/app/api/kb/articles/route.ts` (POST для создания)
- `/app/api/kb/articles/[id]/route.ts` (PUT для редактирования)

Обновить обработку данных для поддержки новых полей:

```tsx
// POST /api/kb/articles
const article = await prisma.kbArticle.create({
  data: {
    title,
    slug,
    content, // HTML из DOCX или Markdown
    contentType: body.contentType || 'TEXT',
    filePath: body.filePath,
    fileSize: body.fileSize,
    originalFileName: body.originalFileName,
    categoryId,
    authorId: session.user.id,
    status: body.status || 'DRAFT',
    tags: {
      create: body.tagIds?.map((tagId: string) => ({
        tagId,
      })),
    },
  },
});
```

---

### 5. **Добавить версионирование файлов**

При редактировании статьи с файлом:

```tsx
// Сохранить текущую версию перед обновлением
await prisma.kbArticleVersion.create({
  data: {
    articleId: article.id,
    title: article.title,
    content: article.content,
    contentType: article.contentType,
    filePath: article.filePath,
    fileSize: article.fileSize,
    originalFileName: article.originalFileName,
    editedBy: session.user.id,
  },
});

// Затем обновить статью
await prisma.kbArticle.update({
  where: { id: article.id },
  data: {
    content: newContent,
    contentType: newContentType,
    filePath: newFilePath,
    fileSize: newFileSize,
    originalFileName: newFileName,
  },
});
```

---

### 6. **Обновить экспорт PDF**

В файле `/app/(dashboard)/knowledge-base/article/[slug]/page.tsx`:

Функция `handleExportPDF` уже работает для TEXT статей.
Для DOCX и PDF:

```tsx
const handleExportPDF = async () => {
  if (article.contentType === 'PDF') {
    // Просто скачать оригинальный PDF
    const link = document.createElement('a');
    link.href = article.filePath!;
    link.download = article.originalFileName!;
    link.click();
    return;
  }

  if (article.contentType === 'DOCX') {
    // Использовать HTML контент для создания PDF
    // (существующая логика с html2canvas уже подойдёт)
  }

  // Для TEXT - существующая логика
};
```

---

## 📁 Структура файлов:

```
/public/uploads/kb/documents/     ← Загруженные файлы
/app/api/kb/upload/route.ts       ← API загрузки ✅
/components/kb/FileUpload.tsx     ← Компонент ✅
/app/(dashboard)/knowledge-base/
  ├── new/page.tsx                 ← Создание (TODO)
  ├── edit/[id]/page.tsx           ← Редактирование (TODO)
  └── article/[slug]/page.tsx      ← Просмотр (TODO)
```

---

## 🎯 Примеры использования:

### Создание статьи с DOCX:

1. Пользователь выбирает "Загрузить документ"
2. Загружает файл `instructions.docx`
3. Файл автоматически конвертируется в HTML
4. Заполняет название, категорию, теги
5. Сохраняет → статья создаётся с `contentType: DOCX`
6. При просмотре отображается как HTML с форматированием

### Замена файла с версионированием:

1. Пользователь открывает редактирование статьи с DOCX
2. Видит текущий файл `instructions-v1.docx`
3. Загружает новый файл `instructions-v2.docx`
4. Старая версия сохраняется в `KbArticleVersion`
5. Статья обновляется с новым файлом
6. В истории версий доступны обе версии

---

## 🔍 Что проверить:

- [ ] Загрузка DOCX работает и конвертируется в HTML
- [ ] Загрузка PDF работает и сохраняется
- [ ] Файлы сохраняются в `/public/uploads/kb/documents/`
- [ ] Статьи с разными contentType отображаются правильно
- [ ] Версионирование файлов работает
- [ ] Экспорт PDF работает для всех типов
- [ ] Поиск работает с DOCX контентом (HTML)
- [ ] Права доступа проверяются при загрузке

---

## 🐛 Возможные проблемы:

### 1. DOCX не конвертируется:
```bash
# Проверьте установку mammoth
npm list mammoth
# Переустановите если нужно
npm install mammoth
```

### 2. PDF не отображается:
- Проверьте путь к файлу
- Убедитесь что `/public/uploads/kb/documents/` существует
- Проверьте права доступа к папке

### 3. Большие файлы не загружаются:
- Next.js имеет лимит на размер body (по умолчанию ~1MB)
- Увеличьте лимит в `next.config.js`:
```js
module.exports = {
  api: {
    bodyParser: {
      sizeLimit: '50mb',
    },
  },
};
```

---

## 📊 Прогресс:

- ✅ База данных обновлена (100%)
- ✅ API загрузки создан (100%)
- ✅ Компонент загрузки создан (100%)
- ⏳ Страница создания статьи (0%)
- ⏳ Страница редактирования (0%)
- ⏳ Страница просмотра (0%)
- ⏳ Версионирование файлов (0%)

**Общий прогресс: ~60%**

---

## 🚀 Следующие шаги:

1. Обновить страницу создания статьи (`/knowledge-base/new`)
2. Добавить переключатель TEXT/FILE
3. Интегрировать FileUpload компонент
4. Обновить API создания для поддержки файлов
5. Обновить страницу просмотра для рендеринга разных типов
6. Добавить версионирование файлов
7. Обновить экспорт PDF для всех типов
8. Тестирование

---

*Документация создана автоматически*
*Версия: 1.0*
*Дата: 12.02.2026*
