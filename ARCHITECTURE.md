# 🏗️ Архитектура системы

## Общий обзор

Система учета присутствия сотрудников - это full-stack веб-приложение, построенное на современном стеке технологий с серверным рендерингом и REST API.

## Технологический стек

### Frontend
- **Next.js 14** - React framework с App Router
- **TypeScript** - Типизация для надежности кода
- **Tailwind CSS** - Утилитарный CSS framework
- **Framer Motion** - Анимации
- **date-fns** - Работа с датами
- **react-hot-toast** - Уведомления

### Backend
- **Next.js API Routes** - Serverless API endpoints
- **Prisma ORM** - Type-safe database client
- **PostgreSQL** - Реляционная база данных
- **NextAuth.js** - Аутентификация и сессии
- **bcryptjs** - Хеширование паролей

## Архитектура данных

### Модель данных (ER-диаграмма)

\`\`\`
┌─────────────────┐
│     User        │
├─────────────────┤
│ id (PK)         │
│ email (unique)  │
│ password        │
│ role            │◄──────┐
│ name            │       │
│ phone           │       │
│ position        │       │
│ skills          │       │
│ avatar          │       │
└─────────────────┘       │
         │                │
         │                │
         ▼                │
┌─────────────────┐       │
│   Attendance    │       │
├─────────────────┤       │
│ id (PK)         │       │
│ userId (FK)     │───────┘
│ date            │
│ status          │
│ unique(userId,  │
│        date)    │
└─────────────────┘
         │
         │
         ▼
┌──────────────────────┐
│ AttendanceRequest    │
├──────────────────────┤
│ id (PK)              │
│ userId (FK)          │──────┐
│ date                 │      │
│ newStatus            │      │
│ reason               │      │
│ status (PENDING/     │      │
│   APPROVED/REJECTED) │      │
└──────────────────────┘      │
                              │
                              └──────► User
\`\`\`

### Статусы посещаемости (AttendanceStatus)
- **OFFICE** - Сотрудник в офисе
- **REMOTE** - Работает удаленно
- **SICK** - Больничный
- **VACATION** - Отпуск
- **DAYOFF** - Отгул

### Статусы запросов (RequestStatus)
- **PENDING** - Ожидает рассмотрения
- **APPROVED** - Подтверждено
- **REJECTED** - Отклонено

## Структура приложения

### App Router (Next.js 14)

\`\`\`
app/
├── (auth)/              # Незащищенные маршруты
│   └── login/          # Страница входа
├── (dashboard)/        # Защищенные маршруты
│   ├── layout.tsx     # Layout с Header и auth check
│   ├── page.tsx       # Главная (календарь)
│   ├── employees/     # Управление сотрудниками (только ADMIN)
│   └── requests/      # Запросы (только ADMIN)
└── api/               # REST API
    ├── auth/          # NextAuth endpoints
    ├── users/         # User CRUD
    ├── attendance/    # Attendance management
    └── requests/      # Change requests
\`\`\`

### Компоненты

#### Календарь
\`\`\`
Calendar (главный компонент)
  ├── DayCell (ячейка дня с аватарками)
  ├── YearView (годовое представление)
  └── DayDetailsModal
        ├── UpdateStatusModal (для ADMIN)
        └── RequestChangeModal (для USER)
\`\`\`

#### UI Components
- **Modal** - Базовый модальный компонент с анимациями
- **Button** - Кнопка с вариантами и состоянием загрузки
- **Header** - Навигация с badge уведомлений

## API Endpoints

### Authentication
- `POST /api/auth/signin` - Вход
- `POST /api/auth/signout` - Выход
- `GET /api/auth/session` - Получить сессию

### Users (ADMIN only для POST/PUT/DELETE)
- `GET /api/users` - Список всех пользователей
- `POST /api/users` - Создать пользователя
- `PUT /api/users/[id]` - Обновить пользователя
- `DELETE /api/users/[id]` - Удалить пользователя

### Attendance
- `GET /api/attendance?view=month&date=YYYY-MM-DD` - Получить за период
- `POST /api/attendance` - Создать/обновить (ADMIN only)

### Requests
- `GET /api/requests?status=PENDING` - Список запросов
- `POST /api/requests` - Создать запрос (USER)
- `PUT /api/requests/[id]` - Approve/Reject (ADMIN only)
- `DELETE /api/requests/[id]` - Удалить запрос

## Алгоритмы

### 1. Рендеринг календаря

\`\`\`typescript
function renderCalendar(currentDate: Date) {
  // 1. Вычисляем границы календарной сетки
  const monthStart = startOfMonth(currentDate)
  const monthEnd = endOfMonth(currentDate)
  const calendarStart = startOfWeek(monthStart, { weekStartsOn: 1 }) // понедельник
  const calendarEnd = endOfWeek(monthEnd, { weekStartsOn: 1 })       // воскресенье
  
  // 2. Генерируем все дни для отображения (обычно 35 или 42 дня)
  const days = eachDayOfInterval({ start: calendarStart, end: calendarEnd })
  
  // 3. Получаем данные посещаемости с сервера
  const attendances = await fetch(\`/api/attendance?view=month&date=\${dateToString(currentDate)}\`)
  
  // 4. Группируем по датам для быстрого доступа
  const attendanceByDate = groupBy(attendances, (att) => dateToString(att.date))
  
  // 5. Рендерим сетку 7x6 (или 7x5)
  return (
    <Grid cols={7}>
      {days.map(day => (
        <DayCell
          date={day}
          isCurrentMonth={isSameMonth(day, currentDate)}
          isToday={isToday(day)}
          attendances={attendanceByDate[dateToString(day)] || []}
        />
      ))}
    </Grid>
  )
}
\`\`\`

### 2. Flow запроса на изменение статуса

\`\`\`
┌─────────────┐
│  Сотрудник  │
└──────┬──────┘
       │ 1. Выбирает дату и статус
       ▼
┌─────────────────────────────┐
│ POST /api/requests          │
│ { date, newStatus, reason } │
└──────────┬──────────────────┘
           │ 2. Создается запись
           ▼
     ┌───────────────┐
     │ DB: requests  │
     │ status=PENDING│
     └───────┬───────┘
             │ 3. Уведомление админу
             ▼
      ┌─────────────┐
      │ Администратор│
      └──────┬───────┘
             │ 4. Approve or Reject
             ▼
┌─────────────────────────────────┐
│ PUT /api/requests/:id           │
│ { action: 'approve' | 'reject' }│
└──────────┬──────────────────────┘
           │
    ┌──────┴───────┐
    │              │
    ▼              ▼
┌─────────┐   ┌──────────┐
│ Approve │   │  Reject  │
└────┬────┘   └─────┬────┘
     │              │
     ▼              ▼
TRANSACTION    UPDATE request
  ├─ UPDATE request    status=REJECTED
  │  status=APPROVED
  └─ UPSERT attendance
     (создать/обновить статус)
\`\`\`

### 3. Обновление статусов (ADMIN)

\`\`\`typescript
async function updateStatuses(userIds: string[], date: Date, status: Status) {
  // Транзакция для атомарности
  await prisma.$transaction(
    userIds.map(userId =>
      prisma.attendance.upsert({
        where: { userId_date: { userId, date } },
        update: { status },
        create: { userId, date, status }
      })
    )
  )
  
  // Unique constraint на (userId, date) гарантирует
  // отсутствие дубликатов
}
\`\`\`

## Безопасность

### Аутентификация (NextAuth.js)

\`\`\`typescript
// Credentials Provider
1. Пользователь вводит email/password
2. Проверка в БД
3. bcrypt.compare(password, hashedPassword)
4. Генерация JWT token
5. Сохранение в cookie (httpOnly, secure)
\`\`\`

### Авторизация (Middleware)

\`\`\`typescript
// В каждом защищенном API route:
const session = await getServerSession(authOptions)

if (!session) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
}

if (session.user.role !== 'ADMIN') {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
}
\`\`\`

### Защита данных
- Пароли хешируются с помощью bcrypt (10 rounds)
- JWT токены с коротким временем жизни
- CSRF защита через NextAuth
- SQL injection защита через Prisma (prepared statements)
- XSS защита через React (автоматический escape)

## Производительность

### Оптимизации

1. **Database Indexing**
   - Index на `attendance.date` для быстрых запросов по диапазону
   - Unique index на `(userId, date)` для предотвращения дубликатов
   - Index на `requests.status` для фильтрации

2. **Data Fetching**
   - Server Components для начального рендеринга
   - Client Components только где нужна интерактивность
   - Минимальная выборка данных (select только нужные поля)

3. **Caching**
   - Next.js автоматически кеширует статические данные
   - Revalidation через `router.refresh()`

4. **Bundle Size**
   - Tree shaking неиспользуемого кода
   - Dynamic imports для модальных окон
   - Оптимизированные изображения через Next.js Image

## Масштабируемость

### Горизонтальное масштабирование

Приложение stateless (без локального состояния), что позволяет:
- Запускать несколько инстансов за load balancer
- Использовать Docker контейнеры
- Деплоить на Vercel/Railway/Render

### Вертикальное масштабирование

- PostgreSQL поддерживает миллионы записей
- Prisma Connection Pooling для оптимизации соединений
- Индексы обеспечивают быстрые запросы даже при росте данных

## Будущие улучшения

1. **Real-time обновления** (WebSockets/Pusher)
2. **Email уведомления** (при создании запроса)
3. **Экспорт данных** (PDF/Excel отчеты)
4. **Интеграция с календарями** (Google Calendar, Outlook)
5. **Mobile приложение** (React Native)
6. **Аналитика** (Dashboard с графиками посещаемости)
7. **Multi-tenancy** (несколько компаний)
8. **Role-based permissions** (более гранулярные права)

## Тестирование

Рекомендуемый стек для тестов:
- **Jest** - Unit тесты
- **React Testing Library** - Component тесты
- **Playwright** - E2E тесты
- **Prisma** - Database mocking

## Деплой

### Production checklist

\`\`\`bash
# 1. Установить переменные окружения
NEXTAUTH_SECRET=<secure-random-string>
DATABASE_URL=<production-db-url>
NEXTAUTH_URL=<production-url>

# 2. Собрать приложение
npm run build

# 3. Применить миграции
npx prisma migrate deploy

# 4. Запустить
npm start
\`\`\`

### Рекомендуемые платформы
- **Vercel** - для Next.js (нативная поддержка)
- **Railway** - для PostgreSQL + Next.js
- **Render** - альтернатива Heroku
- **AWS/GCP/Azure** - для enterprise

---

**Документация актуальна на дату создания проекта**
