# 🔄 Инструкция по обновлению системы

## Новые функции добавлены:
- ✅ Статистика посещаемости
- ✅ Групповое изменение статусов  
- ✅ Фильтры и поиск в календаре
- ✅ Повторяющиеся статусы (правила)
- ✅ **Система отпусков** ⭐ НОВОЕ

## ⚠️ ВАЖНО: Обновление базы данных

После скачивания нового архива выполните следующие шаги:

### Шаг 1: Остановите сервер
```bash
# Нажмите Ctrl+C в терминале где запущен npm run dev
```

### Шаг 2: Обновите базу данных
```bash
# Регенерируйте Prisma Client (ОБЯЗАТЕЛЬНО!)
npx prisma generate

# Примените новую схему к базе данных
npx prisma db push

# Вы должны увидеть что-то вроде:
# ✔ Generated Prisma Client
# The following migration(s) have been created and applied from new schema changes:
# - Added table vacation_requests
# - Added table vacation_approvals
# - Added field vacationDays to users
```

### Шаг 3: Перезапустите сервер
```bash
npm run dev
```

## 🔍 Проверка что всё работает:

1. **Календарь** → Главная страница с фильтрами
2. **Отпуска** → Новый пункт меню (должен открываться без ошибок)
3. **Статистика** → Таблица статистики
4. **Правила** → Повторяющиеся шаблоны

## 🐛 Если ошибка "requests.map is not a function" или подобные:

Это значит что база данных не обновлена. Выполните:

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. ВАЖНО: Сначала генерируем клиент
npx prisma generate

# 3. Удалите базу и пересоздайте
npx prisma migrate reset --force

# 4. Заполните тестовыми данными
npm run prisma:seed

# 5. Запустите
npm run dev
```

## 📊 Новые таблицы в БД:

**vacation_requests** - Запросы на отпуск
- id, userId, startDate, endDate, days, reason, status

**vacation_approvals** - Согласования отпусков
- id, vacationRequestId, approverId, level, status, comment

**users** - Добавлено поле:
- vacationDays (Int, по умолчанию 28) - количество дней отпуска в году

## 🎯 Как использовать систему отпусков:

### Создание запроса (пользователь):
1. Зайдите в "Отпуска"
2. Посмотрите на карточку баланса (доступные дни)
3. Нажмите "Запросить отпуск"
4. Выберите даты начала и окончания
5. Укажите причину (опционально)
6. Отправьте запрос

### Согласование (админ):
1. Зайдите в "Отпуска"
2. Переключитесь на фильтр "Ожидают"
3. Нажмите ✓ для одобрения или ✗ для отклонения
4. При одобрении календарь автоматически заполнится статусом "Отпуск"

### Отмена запроса:
- Можно отменить запросы в статусе PENDING или APPROVED
- При отмене одобренного отпуска записи удаляются из календаря

## ✅ Всё готово!

После выполнения этих шагов все новые функции будут работать.

---

**Если проблемы остаются:**
1. Убедитесь что выполнили `npx prisma generate`
2. Убедитесь что выполнили `npx prisma db push`
3. Проверьте консоль браузера (F12 → Console) на наличие ошибок
4. Проверьте консоль сервера на наличие ошибок API
