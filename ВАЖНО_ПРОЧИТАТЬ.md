# 🚨 ПОЧЕМУ СТАТЬИ НЕ СОЗДАЮТСЯ

## Проблема

Вы нажимаете кнопки:
- ❌ "Отправить на модерацию"
- ❌ "Опубликовать сразу"
- ❌ "Сохранить черновик"

Но **статьи не создаются** и нигде не появляются.

---

## Причина

**Вы НЕ запустили команду:**

```bash
npx prisma generate
```

### Что это значит?

После создания новых таблиц в базе данных (для Базы Знаний), нужно **обновить Prisma Client**, чтобы TypeScript код мог работать с этими таблицами.

**Без этой команды:**
- Prisma не знает о таблицах `kb_article`, `kb_category`, `kb_tag` и т.д.
- Все API запросы выдают ошибку "Cannot read properties of undefined"
- Кнопки работают, но статьи не сохраняются в базу данных

---

## ✅ Решение (3 простых шага)

### Шаг 1: Откройте Terminal (Терминал)

На Mac:
- Откройте **Terminal** (Finder → Программы → Утилиты → Terminal)
- Или нажмите `Cmd + Пробел` и введите "Terminal"

### Шаг 2: Перейдите в папку проекта

```bash
cd ~/Downloads/employee-attendance-system
```

Нажмите Enter.

### Шаг 3: Запустите команду

```bash
npx prisma generate
```

Нажмите Enter.

Вы увидите:
```
✔ Generated Prisma Client (v5.x.x) to ./node_modules/@prisma/client in XXXms
```

### Шаг 4: Перезапустите сервер

Если сервер `npm run dev` запущен:
1. Остановите его: нажмите `Ctrl + C` в терминале где он запущен
2. Запустите заново: `npm run dev`

### Шаг 5: Обновите браузер

Нажмите F5 или Cmd+R в браузере.

---

## 🎉 После выполнения команды

### Теперь будет работать:

✅ **Создание статей**
- Кнопка "Сохранить черновик" → статья создаётся со статусом DRAFT
- Кнопка "Отправить на модерацию" → статья создаётся со статусом PENDING
- Кнопка "Опубликовать сразу" (админ) → статья создаётся со статусом PUBLISHED

✅ **Sidebar**
- Показывает все категории
- Показывает список статей по категориям
- Работают ссылки на статьи

✅ **API endpoints**
- `/api/kb/articles` - создание/редактирование
- `/api/kb/categories` - список категорий
- `/api/kb/search` - поиск
- И все остальные

✅ **Страницы**
- Главная страница БЗ
- Избранное
- Недавние
- Популярные
- Просмотр статьи
- Редактирование

---

## ❓ Вопросы и ответы

### В: Нужно ли запускать эту команду каждый раз?

О: **Нет!** Только один раз после создания новых таблиц в Prisma schema.

### В: Что делать если команда не работает?

О: Убедитесь что:
1. Вы находитесь в правильной папке (`cd ~/Downloads/employee-attendance-system`)
2. У вас установлен Node.js
3. Есть интернет (команда может скачивать бинарные файлы)

### В: Можно ли это пропустить?

О: **Нет!** Без этого База Знаний **вообще не работает**.

---

## 📋 Проверка что всё работает

После `npx prisma generate` и перезапуска сервера:

1. Откройте браузер → http://localhost:3000
2. Перейдите в **База Знаний**
3. Нажмите **Создать статью**
4. Заполните форму:
   - Заголовок: "Тестовая статья"
   - Категория: выберите любую
   - Содержимое: "Это тест"
5. Нажмите **"Сохранить черновик"**

**Должно появиться:**
- ✅ Уведомление "Черновик сохранён!"
- ✅ Перенаправление на главную страницу
- ✅ Статья появится в Sidebar (если категория раскрыта)

---

## 🔥 ЭТО ВАЖНО!

**Без команды `npx prisma generate` НИЧЕГО НЕ БУДЕТ РАБОТАТЬ!**

Это не баг и не ошибка в коде.
Это стандартный процесс работы с Prisma ORM.

**Запустите команду ПРЯМО СЕЙЧАС!** ⬆️

---

*Если после выполнения команды проблемы остались - напишите, буду разбираться дальше.*
