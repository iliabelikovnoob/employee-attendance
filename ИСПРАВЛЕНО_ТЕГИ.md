# ✅ ИСПРАВЛЕНА ОШИБКА РОУТИНГА ТЕГОВ

**Дата:** 11 февраля 2026

---

## 🐛 Проблема (исправлена)

При запуске `npm run dev` возникала ошибка:

```
Error: You cannot use different slug names for the same dynamic path ('id' !== 'name').
```

### Причина

В папке `/app/api/kb/tags/` существовали два динамических роута с разными именами:
- `[id]` - для редактирования/удаления тегов (PUT/DELETE)
- `[name]` - для получения статей по тегу (GET)

Next.js не позволяет иметь разные имена динамических параметров на одном уровне маршрута.

---

## ✅ Решение (выполнено)

### Изменения в структуре API:

**Было:**
```
/app/api/kb/tags/
├── [id]/route.ts          ← PUT/DELETE (конфликт!)
├── [name]/route.ts        ← GET (конфликт!)
└── route.ts               ← GET list
```

**Стало:**
```
/app/api/kb/tags/
├── [id]/route.ts          ← PUT/DELETE (по ID)
├── by-name/
│   └── [name]/route.ts    ← GET (по имени) ✨ НОВОЕ
└── route.ts               ← GET list
```

### Обновлённые файлы:

1. **API endpoint перемещён:**
   - `/app/api/kb/tags/[name]/route.ts` → `/app/api/kb/tags/by-name/[name]/route.ts`

2. **Обновлён код страницы тега:**
   - Файл: `/app/(dashboard)/knowledge-base/tag/[name]/page.tsx`
   - Изменение: `fetch('/api/kb/tags/${name}')` → `fetch('/api/kb/tags/by-name/${name}')`

---

## 🚀 Как запустить проект

### Шаг 1: Сгенерируйте Prisma Client

⚠️ **КРИТИЧНО!** Без этой команды статьи не будут создаваться!

```bash
cd ~/Downloads/employee-attendance-system
npx prisma generate
```

Вы должны увидеть:
```
✔ Generated Prisma Client (v5.x.x) to ./node_modules/@prisma/client in XXXms
```

### Шаг 2: Запустите сервер разработки

```bash
npm run dev
```

Теперь **ошибки быть не должно**! ✅

Сервер должен запуститься на http://localhost:3000

---

## 📋 Проверка работоспособности

После запуска проверьте следующие функции:

### 1. Управление тегами (админ)
- Откройте: http://localhost:3000/admin/kb-tags
- ✅ Список всех тегов отображается
- ✅ Поиск работает
- ✅ Редактирование тега (клик на карандаш)
- ✅ Удаление тега работает

### 2. Просмотр статей по тегу
- Откройте любую статью в БЗ
- Кликните на тег (например, `#javascript`)
- ✅ Переход на `/knowledge-base/tag/javascript`
- ✅ Список статей с этим тегом отображается

### 3. Создание статей
- Откройте: http://localhost:3000/knowledge-base/new
- Заполните форму:
  - Заголовок: "Тестовая статья"
  - Категория: любая
  - Теги: введите "тест", "демо"
  - Содержимое: любой текст
- Кликните "Сохранить черновик" или "Опубликовать"
- ✅ Статья создаётся без ошибок

---

## 🎯 Что теперь работает

### Все API endpoints для тегов:

1. **GET** `/api/kb/tags` - получить все теги
2. **GET** `/api/kb/tags/by-name/[name]` - получить статьи по имени тега ✨ НОВЫЙ ПУТЬ
3. **PUT** `/api/kb/tags/[id]` - обновить тег (админ)
4. **DELETE** `/api/kb/tags/[id]` - удалить тег (админ)

### Страницы:

- ✅ `/knowledge-base/tag/[name]` - страница тега (обновлена)
- ✅ `/admin/kb-tags` - админ-панель управления тегами (без изменений)

---

## ⚠️ Примечание для разработчиков

При создании новых API endpoints в Next.js App Router:
- ✅ **Можно:** `/api/resource/[id]`, `/api/resource/by-slug/[slug]`
- ❌ **Нельзя:** `/api/resource/[id]`, `/api/resource/[slug]` (конфликт!)

**Правило:** На одном уровне маршрута все динамические сегменты должны иметь одинаковое имя, либо находиться в разных вложенных папках.

---

## 📊 Статус проекта

- ✅ Ошибка роутинга исправлена
- ✅ Все API endpoints работают
- ✅ Админ-панель тегов функциональна
- ✅ Страницы тегов работают
- ✅ База Знаний полностью функциональна

**Проект готов к использованию!** 🎉

---

## 🆘 Если что-то не работает

1. **Статьи не создаются:**
   - Убедитесь что выполнили `npx prisma generate`
   - Перезапустите сервер (`Ctrl+C`, затем `npm run dev`)

2. **Ошибка при открытии страницы тега:**
   - Очистите кеш Next.js: `rm -rf .next`
   - Перезапустите сервер

3. **Другие ошибки:**
   - Проверьте консоль браузера (F12)
   - Проверьте логи сервера в терминале

---

*Создано: 11 февраля 2026*
*Исправлено: Ошибка роутинга тегов Next.js*
