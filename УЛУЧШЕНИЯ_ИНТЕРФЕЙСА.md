# ✅ Улучшения интерфейса базы знаний

**Дата:** 12 февраля 2026
**Изменения:**
1. Добавлена кнопка загрузки изображений на странице создания статьи
2. Добавлен счетчик статей на модерации в сайдбаре

---

## 🎯 Изменение 1: Кнопка загрузки изображений при создании

### Проблема:
- ❌ При создании статьи не было кнопки для загрузки изображений
- ❌ Кнопка была доступна только в редакторе существующих статей
- ❌ Приходилось сохранять черновик, затем редактировать для добавления изображений

### Решение:
✅ Добавлена кнопка "Добавить изображение" на страницу создания статьи

### Что добавлено:

#### 1. **State и refs:**
```typescript
const [uploadingImage, setUploadingImage] = useState(false);
const fileInputRef = useRef<HTMLInputElement>(null);
```

#### 2. **Функция загрузки изображения:**
```typescript
const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (!file) return;

  // Проверка типа и размера
  if (!file.type.startsWith('image/')) {
    alert('Пожалуйста, выберите изображение');
    return;
  }

  if (file.size > 10 * 1024 * 1024) {
    alert('Размер файла не должен превышать 10MB');
    return;
  }

  // Загрузка через API
  const response = await fetch('/api/kb/media/upload', {
    method: 'POST',
    body: formData,
  });

  // Вставка markdown в позицию курсора
  const markdownImage = `\n![${imageName}](${imageUrl})\n`;
  // ...
};
```

#### 3. **UI кнопки:**
```typescript
<button
  type="button"
  onClick={() => fileInputRef.current?.click()}
  disabled={uploadingImage}
  className="flex items-center gap-1 px-3 py-1.5 text-xs bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-300 rounded-lg hover:bg-blue-200 dark:hover:bg-blue-800 transition-colors disabled:opacity-50"
>
  <IoImageOutline className="w-4 h-4" />
  {uploadingImage ? 'Загрузка...' : 'Добавить изображение'}
</button>
<input
  ref={fileInputRef}
  type="file"
  accept="image/*"
  onChange={handleImageUpload}
  className="hidden"
/>
```

### Как работает:

1. Пользователь нажимает кнопку "Добавить изображение"
2. Открывается диалог выбора файла
3. Изображение загружается на сервер через API
4. Markdown-код `![название](url)` вставляется в позицию курсора
5. Изображение сразу доступно в предпросмотре

### Где находится кнопка:

Под полем ввода текста, справа от подсказки "Поддерживается Markdown":

```
[ℹ️ Поддерживается Markdown]    [🖼️ Добавить изображение]
```

### Обновлена подсказка Markdown:

Добавлена строка в выпадающую подсказку:
```
🖼️ Нажмите "Добавить изображение" чтобы загрузить картинку (до 10MB)
```

---

## 🎯 Изменение 2: Счетчик статей на модерации

### Проблема:
- ❌ Не было видно сколько статей ждут одобрения
- ❌ Приходилось заходить на страницу "Ждут публикации" чтобы узнать
- ❌ Нет уведомлений о новых статьях на модерации

### Решение:
✅ Добавлен счетчик рядом с "Ждут публикации" в сайдбаре

### Что добавлено:

#### 1. **State для счетчика:**
```typescript
const [pendingCount, setPendingCount] = useState(0);
```

#### 2. **Загрузка счетчика:**
```typescript
const fetchData = async () => {
  // ...
  const pendingRes = await fetch('/api/kb/pending?limit=1000');

  if (pendingRes.ok) {
    const pendingData = await pendingRes.json();
    setPendingCount(Array.isArray(pendingData) ? pendingData.length : 0);
  }
};
```

#### 3. **Отображение счетчика:**
```typescript
<Link href="/knowledge-base/pending">
  <div className="flex items-center gap-3">
    <IoHourglassOutline className="w-5 h-5 text-yellow-500" />
    <span>Ждут публикации</span>
  </div>
  {pendingCount > 0 && (
    <span className="px-2 py-0.5 bg-yellow-100 dark:bg-yellow-900/30 text-yellow-700 dark:text-yellow-300 rounded-full text-xs font-medium">
      {pendingCount}
    </span>
  )}
</Link>
```

### Как работает:

1. При загрузке сайдбара запрашиваются статьи на модерации
2. Подсчитывается их количество
3. Если есть статьи PENDING, показывается счетчик (жёлтый бейдж)
4. При одобрении статьи срабатывает событие `kb-refresh`
5. Сайдбар обновляется, счетчик уменьшается

### Визуальный дизайн:

```
⏳ Ждут публикации    [5]
                      ↑
                жёлтый бейдж
```

- Жёлтый фон (соответствует статусу "на модерации")
- Округлый бейдж
- Компактный размер (text-xs)
- Показывается только если есть статьи

### Автоматическое обновление:

Счетчик обновляется при:
- ✅ Загрузке страницы
- ✅ Создании новой статьи и отправке на модерацию
- ✅ Одобрении статьи (событие `kb-refresh`)
- ✅ Отклонении статьи
- ✅ Удалении статьи

---

## 📂 Изменённые файлы:

### 1. `/app/(dashboard)/knowledge-base/new/page.tsx`
- ✅ Добавлены state: `uploadingImage`, `fileInputRef`
- ✅ Добавлена функция `handleImageUpload`
- ✅ Добавлен импорт `IoImageOutline`
- ✅ Добавлена кнопка "Добавить изображение"
- ✅ Добавлен скрытый input для выбора файла
- ✅ Обновлена подсказка Markdown

### 2. `/components/kb/KbSidebar.tsx`
- ✅ Добавлен state: `pendingCount`
- ✅ Обновлена функция `fetchData` (загрузка счетчика)
- ✅ Обновлена ссылка "Ждут публикации" (добавлен счетчик)

---

## 🚀 Как использовать:

### Добавление изображений при создании:

1. Откройте страницу создания статьи
2. Введите текст
3. Поставьте курсор в нужное место
4. Нажмите "Добавить изображение"
5. Выберите изображение (до 10MB)
6. ✅ Изображение загружается и вставляется как markdown

### Просмотр статей на модерации:

1. Откройте сайдбар
2. Посмотрите на "Ждут публикации"
3. Увидите жёлтый бейдж с числом (если есть статьи)
4. Нажмите на ссылку для просмотра всех статей
5. После одобрения счетчик автоматически уменьшится

---

## ⚡ Преимущества:

### Для пользователей:
1. **Быстрое добавление изображений:**
   - ✅ Не нужно сохранять черновик
   - ✅ Не нужно переходить в редактор
   - ✅ Сразу при создании статьи

2. **Видимость статей на модерации:**
   - ✅ Всегда видно сколько статей ждут
   - ✅ Не нужно заходить на отдельную страницу
   - ✅ Привлекает внимание админа

### Для администраторов:
1. **Уведомление о новых статьях:**
   - ✅ Жёлтый бейдж привлекает внимание
   - ✅ Сразу видно количество
   - ✅ Счетчик обновляется автоматически

2. **Контроль модерации:**
   - ✅ Легко отслеживать накопление статей
   - ✅ Понятно когда нужно проверить
   - ✅ Счетчик уменьшается после одобрения

---

## 📊 Технические детали:

### Загрузка изображений:
- **API:** `/api/kb/media/upload`
- **Метод:** POST (FormData)
- **Лимит:** 10MB
- **Форматы:** Все image/* (jpg, png, gif, webp и т.д.)
- **Вставка:** Markdown синтаксис `![название](url)`
- **Позиция:** В место курсора в textarea

### Счетчик статей:
- **API:** `/api/kb/pending?limit=1000`
- **Подсчет:** `Array.length` на клиенте
- **Обновление:** Через событие `kb-refresh`
- **Видимость:** Только если `pendingCount > 0`
- **Фильтрация:** Админы видят все, авторы - только свои

### События обновления:
```typescript
// После одобрения статьи
window.dispatchEvent(new Event('kb-refresh'));

// Сайдбар слушает событие
window.addEventListener('kb-refresh', handleRefresh);
```

---

## 🎨 Визуальный дизайн:

### Кнопка загрузки изображения:
- Синий фон (`bg-blue-100 dark:bg-blue-900`)
- Иконка 🖼️ (`IoImageOutline`)
- Disabled состояние при загрузке
- Текст меняется: "Добавить изображение" → "Загрузка..."

### Счетчик статей:
- Жёлтый фон (`bg-yellow-100 dark:bg-yellow-900/30`)
- Жёлтый текст (`text-yellow-700 dark:text-yellow-300`)
- Округлый бейдж (`rounded-full`)
- Компактный размер (`text-xs`)
- Справа от текста ссылки

---

## 🎉 Итоги:

### ✅ Добавлено:
1. Кнопка "Добавить изображение" при создании статьи
2. Счетчик статей на модерации в сайдбаре
3. Автоматическое обновление счетчика

### ✅ Улучшен UX:
1. Не нужно сохранять черновик для добавления изображений
2. Видно количество статей на модерации без перехода
3. Админы сразу замечают новые статьи

### ✅ Результат:
- Более удобное создание статей с изображениями
- Лучшая видимость статей на модерации
- Быстрая реакция админов на новые статьи

---

*Улучшения завершены!*
*Версия: 2.4*
*Дата: 12.02.2026*
